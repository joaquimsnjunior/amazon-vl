# Example htpasswd file for amazon-vl
# Format: username:md5_crypt_hash
#
# Generate hash using:
#   openssl passwd -1 -salt "$(openssl rand -base64 6)" "password"
#   or: htpasswd -nbm username password | cut -d: -f2
#   or: https://unix4lyfe.org/crypt/
#
# Default credentials (for development only):
#   User: joaquim
#   Password: amazon
#
joaquim:$1$neD1XEAG$WylfbCkcn9psU0o467.AM1
